#!/usr/bin/env bash
# Точка входа в квест. Корень проекта — родитель каталога bin/.
set -euo pipefail
_PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec bash "$_PROJECT_ROOT/lib/run.sh" "$@"
